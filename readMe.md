# Webpack-tempale - base webpack config to build a big applications with separate dev and prod configs 

### Use webpack-config:
  * npm i / yarn 
  * start base app
    * in production mode: npm run build / yarn build
    * in development mode: npm run serve / yarn serve
  